# EdgeConfig 配置发布服务项目设计

> 这是一份给项目实现模型的真实工程说明。项目目录必须是全新的空目录，不得读取或复用其它任务资料。

## 工作目录

Work only in: D:\xiangm\wogo\wogo-production-v2\projects\PROD21-go-project

## 业务背景

EdgeConfig 是一个供现场运维工程师和边缘设备代理使用的配置发布服务。运维人员为一个站点登记设备、提交配置版本并发起滚动发布；设备代理定期拉取属于自己的发布计划，汇报下载、应用和健康检查状态。服务需要在进程重启后恢复未完成的发布，保证已经确认的设备状态不会因为重复事件倒退，并把每次状态变化写入可追加的事件日志，方便后续审计和恢复。

项目不是简单的 CRUD。一次发布会经过 draft、queued、dispatching、applying、verified、completed 或 failed 状态，设备级状态和发布级聚合状态相互影响。代理可能离线、重复上报、乱序上报或在应用配置后马上重启；HTTP API、内存调度器、持久化层、恢复器和 CLI smoke 入口需要围绕同一套状态模型协作。

## 角色与主要流程

1. 运维工程师通过 HTTP API 创建站点和设备，提交带有 schema 版本、参数校验结果和变更摘要的配置版本。
2. 工程师选择一组设备创建 rollout，服务将 rollout 和设备目标写入 JSONL 持久化文件，并按设备能力生成待发送的 assignment。
3. 设备代理调用拉取接口取得 assignment，回报 accepted、downloaded、applied、health_checked 或 rejected 事件。服务为每个设备维护当前版本、尝试次数、最后错误和心跳时间。
4. 聚合器根据设备事件推进 rollout 的整体状态；当所有目标通过健康检查时发布完成，超出重试预算或出现不可恢复错误时发布失败。事件写入成功后才能向调用方返回确认，恢复过程必须能从快照和事件日志重建调度队列。
5. 管理 CLI 提供初始化、启动 HTTP 服务、查看 rollout、重放事件和执行 smoke 的入口。README 要给出不依赖外部服务的本地运行步骤。

## 需要落地的模块

- `domain`：站点、设备、配置版本、rollout、assignment、设备事件及状态转移规则；对非法转移返回带上下文的错误。
- `persistence`：顺序写入的事件日志、周期性快照、原子临时文件替换、损坏尾记录处理、启动恢复和版本校验。接口要允许内存实现用于单元测试。
- `rollout`：创建发布、选择设备、分批调度、重试预算、重复/乱序事件去重和聚合状态计算；调度器要有可取消的后台循环。
- `agent`：设备拉取、租约/确认、应用结果和健康检查处理，保证同一设备的状态更新经过统一服务链路。
- `api`：使用 Go 标准库 `net/http` 提供 JSON API，包含请求解码、验证、错误响应、幂等请求键和查询接口；不要引入需要额外服务才能运行的框架。
- `recovery`：启动时读取快照和事件、校验事件序列、重建内存索引及待调度工作，并暴露恢复报告。
- `cli` 与 `cmd/edgeconfig`：可执行入口、配置加载、优雅关闭、结构化日志和 smoke 所需的最小命令。
- `internal/testutil`：只放有业务意义的测试夹具、事件序列和 HTTP 客户端辅助，不生成重复占位代码。

## 交付质量要求

- 使用 Go 1.23、严格 UTF-8、标准库优先；如果确有依赖，固定版本并提交完整 `go.sum`。
- 建立完整 Git 仓库，默认分支为 `main`，至少有一个说明业务链路的根提交。
- 生产代码必须真实连通 HTTP/CLI、领域状态机、持久化、恢复和调度路径，达到至少 24 个真实生产 Go 文件和 2400 行连接的生产代码；不能用重复包装器、空函数、死代码、编号函数或占位文件凑数。
- 提供至少 5 个有业务意义的测试文件，覆盖状态转移、日志恢复、乱序/重复设备事件、API 流程、调度取消或重试等不同链路。测试必须真正调用生产实现。
- README 要写出架构、状态流转、持久化格式、运行命令和一个从创建配置到设备健康确认的完整示例。
- 增加稳定的本地 smoke 入口，能够在空目录中初始化临时数据、启动服务或调用业务服务并打印可检查的成功结果；不要依赖云服务、随机网络或预置凭据。
- 在项目目录中运行宿主机 `go test ./...`、`go vet ./...`、`go build ./...`，并准备可在 `golang:1.23.12` 容器内使用 `sh -c` 执行的同样检查。

## 实现顺序

先建立模块边界、领域模型和持久化/恢复契约，再接入 rollout 调度、代理事件处理、HTTP API 和 CLI。每一层都要有实际调用者和错误传播；不要先生成大量孤立文件。完成后自行做一次独立的准入检查，修复编译、测试、恢复和 smoke 问题，确认 Git 工作树干净并把最终状态留在 `main`。

## 生成阶段自检

- 真实角色、业务动作、状态变化、持久化边界和下游影响都已实现：是
- 生产文件与生产行数来自可达业务链路，而不是填充：必须是
- 测试覆盖多个模块且调用真实生产入口：必须是
- 项目可在无外部服务下运行并恢复：必须是
- 禁止领域：否
